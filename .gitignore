# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust
src-tauri/target/

# 环境基座（内置 Git/JDK/Node 便携运行时）随仓库跟踪；
# node_modules 由上方全局规则忽略（第三方包缓存，可重建）

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 开发期临时调试产物（已归档到 scripts/legacy/ 留档）
# OCR 临时输出
ocr_*.txt
# 像素扫描临时输出
pixel_*.txt
# 屏幕区域扫描临时输出
scan_*.txt
# Vite 构建日志
vite.log
# Rust 编译试验产物
min_test.exe
min_test.pdb
# 0 字节误建文件
/-
# 一次性标记文件
*.flag

# 备份文件
*.bak
*.before-rewrite

# ohpm 面板运行时缓存（IDE 拉取数据，可重建）
_ide.json

# 开发期截图与测试脚手架（不对外发布）
screenshots/
_e2e_new_harmony/
_lsp_test/

# 内置运行时与大型资源（约 1GB 构建产物，不随仓库分发）：
# - runtime/     便携版 Node + JDK + Git（构建/打包时需要，CI 自动下载）
# - resources/   种子知识库 knowledge.db + embedding 模型 + CUDA 运行时
# - portable-build/ 绿色版构建产物
# 本机构建需保留这些目录；克隆用户请参考 README 从 Release 或官方源获取。
src-tauri/runtime/
src-tauri/resources/
portable-build/
